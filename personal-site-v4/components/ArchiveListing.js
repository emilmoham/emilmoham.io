import blogDefault from '../public/blog_default.png'

export default function ArchiveListing({ props }) {
    return (
        <div >
            <div>
                <img src={blogDefault} />
            </div>
        </div>
    );
}